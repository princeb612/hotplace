# @author Soo Han, Kim (princeb612.kr@gmail.com)
# @desc
# Revision History
# Date         Name                Description
#

file (GLOB FILEGROUP_SDK_HEADER_TEST
    ${PROJECT_SOURCE_DIR}/sdk/test/unittest/*.hpp
)

file (GLOB FILEGROUP_SDK_SOURCE_TEST
    ${PROJECT_SOURCE_DIR}/sdk/test/unittest/*.cpp
)

file (GLOB FILEGROUP_SDK_SOURCE_TEST_LINUX
)

file (GLOB FILEGROUP_SDK_SOURCE_TEST_WIN32
)

if (UNIX)
set (FILEGROUP_SDK_TEST ${FILEGROUP_SDK_HEADER_TEST} ${FILEGROUP_SDK_SOURCE_TEST} ${FILEGROUP_SDK_SOURCE_TEST_LINUX})
set (PLATFORM_LIB_SDK_TEST )
elseif (WIN32)
set (FILEGROUP_SDK_TEST ${FILEGROUP_SDK_HEADER_TEST} ${FILEGROUP_SDK_SOURCE_TEST} ${FILEGROUP_SDK_SOURCE_TEST_WIN32})
set (PLATFORM_LIB_SDK_TEST )
endif ()

if (SUPPORT_STATIC GREATER 0)
    set (PROJECT_SDK_MODULE_DEPENDENCIES_TEST_STATIC ${PROJECT_SDK_MODULE_BASE}-static ${PROJECT_SDK_MODULE_IO}-static)
    makelibdep(${PROJECT_SDK_MODULE_TEST} STATIC FILEGROUP_SDK_TEST FILEGROUP_SDK_HEADER_TEST PROJECT_SDK_MODULE_DEPENDENCIES_TEST_STATIC PLATFORM_LIB_SDK_TEST)
endif ()

if (SUPPORT_SHARED GREATER 0)
    set (PROJECT_SDK_MODULE_DEPENDENCIES_TEST_SHARED ${PROJECT_SDK_MODULE_BASE}-shared ${PROJECT_SDK_MODULE_IO}-shared)
    makelibdep(${PROJECT_SDK_MODULE_TEST} SHARED FILEGROUP_SDK_TEST FILEGROUP_SDK_HEADER_TEST PROJECT_SDK_MODULE_DEPENDENCIES_TEST_SHARED PLATFORM_LIB_SDK_TEST)
endif ()
