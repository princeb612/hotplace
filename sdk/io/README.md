### io

```mermaid
mindmap
  root((io))
    CBOR
      COSE
    multiplexer
     epoll
     IOCP
    utilities
      payload
        HTTP/2 frame
        QUIC packet
      MLFQ
      parser
      zlib
        content-encoding
      filestream
```
