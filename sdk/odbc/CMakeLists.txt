# @author Soo Han, Kim (princeb612.kr@gmail.com)
# @desc
# Revision History
# Date         Name                Description
#

file (GLOB FILEGROUP_SDK_HEADER_ODBC
    ${PROJECT_SOURCE_DIR}/sdk/odbc/*.hpp
    ${PROJECT_SOURCE_DIR}/sdk/odbc/basic/*.hpp
)

file (GLOB FILEGROUP_SDK_SOURCE_ODBC
    ${PROJECT_SOURCE_DIR}/sdk/odbc/basic/*.cpp
)

file (GLOB FILEGROUP_SDK_SOURCE_ODBC_LINUX
)

file (GLOB FILEGROUP_SDK_SOURCE_ODBC_WIN32
)

file (GLOB FILEGROUP_SDK_SOURCE_ODBC_WIN32_UNICODE
)

set_source_files_properties(${FILEGROUP_SDK_SOURCE_ODBC_WIN32_UNICODE} PROPERTIES SKIP_PRECOMPILE_HEADERS ON)

if (UNIX)
set (FILEGROUP_SDK_ODBC ${FILEGROUP_SDK_HEADER_ODBC} ${FILEGROUP_SDK_SOURCE_ODBC} ${FILEGROUP_SDK_SOURCE_ODBC_LINUX})
set (PLATFORM_LIB_SDK_ODBC )
elseif (WIN32)
set (FILEGROUP_SDK_ODBC ${FILEGROUP_SDK_HEADER_ODBC} ${FILEGROUP_SDK_SOURCE_ODBC} ${FILEGROUP_SDK_SOURCE_ODBC_WIN32} ${FILEGROUP_SDK_SOURCE_ODBC_WIN32_UNICODE})
set (PLATFORM_LIB_SDK_ODBC )
endif ()

if (SUPPORT_STATIC GREATER 0)
    set (PROJECT_SDK_MODULE_DEPENDENCIES_ODBC_STATIC ${PROJECT_SDK_MODULE_BASE}-static)
    makelibdep(${PROJECT_SDK_MODULE_ODBC} STATIC FILEGROUP_SDK_ODBC FILEGROUP_SDK_HEADER_ODBC PROJECT_SDK_MODULE_DEPENDENCIES_ODBC_STATIC PLATFORM_LIB_SDK_ODBC)
endif ()

if (SUPPORT_SHARED GREATER 0)
    set (PROJECT_SDK_MODULE_DEPENDENCIES_ODBC_SHARED ${PROJECT_SDK_MODULE_BASE}-shared)
    makelibdep(${PROJECT_SDK_MODULE_ODBC} SHARED FILEGROUP_SDK_ODBC FILEGROUP_SDK_HEADER_ODBC PROJECT_SDK_MODULE_DEPENDENCIES_ODBC_SHARED PLATFORM_LIB_SDK_ODBC)
endif ()
